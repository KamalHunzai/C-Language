#include <stdio.h>
// defining constant as preprocessor
#define LEN 10

int main() {
// Delcaring a constant via const keyword
const WIDTH = 20;
int area;

area = LEN * WIDTH;
printf("Area is : %d \n",area);
getch();
}
